# ==============================================
# ServeSync Environment Variables (Example)
# COPY THIS FILE TO .env AND FILL IN THE VALUES
# ==============================================

# --- Server Configuration ---
PORT=3100
NODE_ENV=development
# Isolate or 'true' to use Real APIs (Google Maps, etc.)
USE_REAL_APIS=false 

# --- Database (PostgreSQL) ---
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="postgresql://user:password@localhost:5432/servesync_db"

# --- Security (JWT) ---
# Generate a secure random string for signing tokens
JWT_SECRET="your_super_secret_jwt_key_here"

# --- External APIs (Required for Real Mode) ---

# 1. Google Maps Platform
# Needed for: Geocoding, Distance Matrix (Real-time tracking)
GOOGLE_MAPS_API_KEY="AIzaSy..."

# 2. Push Notifications (FCM / OneSignal)
# Needed for: Sending mobile alerts to Drivers/Customers
FCM_SERVER_KEY="AAAA..."
ONESIGNAL_APP_ID="..."

# 3. Payments (Nigerian Gateways)
# Pick your provider and fill the keys
PAYSTACK_SECRET_KEY="sk_test_..."
FLUTTERWAVE_SECRET_KEY="FLWSECK_..."

# 4. Email Service (SendGrid / AWS SES)
# Needed for: Welcome emails, Receipts
SENDGRID_API_KEY="SG...."
EMAIL_FROM="no-reply@servesync.com"
